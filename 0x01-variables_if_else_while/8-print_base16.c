#include <stdio.h>

/**
 * main - Base 16
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;
	int y;

	for (x = 48; x <= 57; x++)
	{
		putchar(x);
	}
	for (y = 97; x <= 102; y++)
	{
		putchar(y);
	}
	putchar('\n');
	return (0);
}
